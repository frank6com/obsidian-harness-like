{
  "name": "harness-like-plugin",
  "version": "0.35.3",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@deepseek-ai/cordis": "4.0.1",
    "@harness-like/harness-base": "workspace:*",
    "@harness-like/obsidian-adapter": "workspace:*",
    "@harness-like/plugin-runtime": "workspace:*",
    "dompurify": "^3.4.13",
    "marked": "^18.0.9"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "esbuild": "^0.28.2",
    "obsidian": "^1.13.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
